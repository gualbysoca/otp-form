extends ./layout.jade

block contenido
    script(src="/javascripts/otp-frontend.js")
    h1 Recibimos el número de telefono #{msisdn} en el formulario 1 y enviamos el SMS desde el backend, luego quedamos a la espera del mensaje en el formulario 2...
    form(action="/", method="POST" id="otp-form")
        div(class="form-group")
            label(for="otp-input") Código PIN recibido
            input(type="number" name="otp-input" placeholder="El código PIN se leerá automáticamente" id="otp-input" pattern="^([0-9]+)$" class="form-control" is="one-time-code" autocomplete="one-time-code" required)
        div
            input(type="submit", value="Enviar" class="btn btn-info")

    
    mwc-snackbar(id="snackbar")
 


